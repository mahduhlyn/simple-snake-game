#include <iostream>
#include <windows.h>
#include <conio.h>
using namespace std;

void Setup();
void Draw();
void Input();
void Logic();

bool gameOver;
const int width = 30;
const int height = 20;
int x, y, fruitX, fruitY, score;
int tailX[100], tailY[100];
int nTail;

enum eDirection {STOP = 0, LEFT, RIGHT, UP, DOWN};
eDirection dir;
